{
  "name": "runebender-web",
  "version": "0.0.1",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm": "cd core && wasm-pack build --target web --out-dir ../wasm --release",
    "wasm:debug": "cd core && wasm-pack build --target web --out-dir ../wasm --dev",
    "gf-sidebar": "node scripts/generate-gf-sidebar-data.mjs",
    "demo-font": "sh scripts/update-demo-font.sh"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
